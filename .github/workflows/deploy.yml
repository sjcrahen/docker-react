name: Deploy Frontend

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - run: docker build -t scrahen/docker-react -f Dockerfile.dev .
      - run: docker run -e CI=true scrahen/docker-react npm run test
